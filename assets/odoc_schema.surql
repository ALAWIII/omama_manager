DEFINE TABLE IF NOT EXISTS document SCHEMAFULL;
DEFINE FIELD IF NOT EXISTS id ON TABLE document TYPE string;
DEFINE FIELD IF NOT EXISTS content ON TABLE document TYPE string;
DEFINE FIELD IF NOT EXISTS embedding ON TABLE document TYPE array<float>;
DEFINE INDEX OVERWRITE embedding_index ON TABLE document
    FIELDS embedding HNSW DIMENSION 768 DIST COSINE;
